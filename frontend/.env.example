# Server-side Directus URL (preferred for server fetches)
DIRECTUS_URL=https://your-directus-domain
# Server-side Directus token used as: Authorization: Bearer <token>
DIRECTUS_ACCESS_TOKEN=your-directus-access-token
# Public Directus URL (used by client-side code when needed)
NEXT_PUBLIC_DIRECTUS_URL=https://your-directus-domain
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=pk.your-mapbox-public-token
NEXT_PUBLIC_MAPBOX_STYLE=mapbox://styles/mapbox/dark-v11
